/**
 * V1 LED — flat sponsor array. Presentation-only, matches the
 * "Official Partners" strip already in production /display.
 */
export const SPONSOR_NAMES: string[] = ["NEXUS", "VELOCITY", "PRIMESTEEL"];
