/**
 * @workspace/shared-ui — future platform UI service.
 *
 * Destination for BidWar chrome, page-chrome, SportsShell primitives shared by
 * sports and auction apps. Empty by design in this migration pass.
 */

export {};
