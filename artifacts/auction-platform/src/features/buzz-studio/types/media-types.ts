export interface PlayerAsset {
  id: string;
  imageUrl?: string;
  qualityScore?: number;
  isEnhanced?: boolean;
  hasTransparency?: boolean;
}

export interface TeamAsset {
  id: string;
  imageUrl?: string;
  qualityScore?: number;
  isEnhanced?: boolean;
  hasTransparency?: boolean;
}

export interface TournamentAsset {
  id: string;
  imageUrl?: string;
  qualityScore?: number;
  isEnhanced?: boolean;
  hasTransparency?: boolean;
}
