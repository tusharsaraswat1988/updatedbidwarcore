/**
 * @workspace/sports-cricket — Cricket sport domain.
 *
 * Temporary facade: re-exports @workspace/scoring-core until engine code is owned here.
 * Franchise roster reads belong to @workspace/player-registry (platform service).
 */

export * from "@workspace/scoring-core";
