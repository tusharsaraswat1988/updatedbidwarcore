/**
 * Buzz Studio — Template Types
 *
 * Canonical enum of every template that exists or is planned.
 * This enum is the authoritative identifier used across the registry,
 * contracts layer, future dashboard, and future PNG renderer.
 */

export enum BuzzTemplateType {
  PLAYER_SPOTLIGHT = "player_spotlight",
  SOLD_PLAYER = "sold_player",
  TOP_BUYS = "top_buys",
  TEAM_REVEAL = "team_reveal",
  AUCTION_SUMMARY = "auction_summary",
  MVP_CARD = "mvp_card",
  TOURNAMENT_LAUNCH = "tournament_launch",
}
